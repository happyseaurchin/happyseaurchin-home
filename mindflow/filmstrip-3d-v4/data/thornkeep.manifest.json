{
  "owner": "thornkeep-gm",
  "names": ["thornkeep-world", "thornkeep-rules", "thornkeep-protocol", "thornkeep-events"],
  "note": "Consumed by filmstrip-3d's URL-param dispatcher. If /api/pscale-walk is deployed with SUPABASE_URL + SUPABASE_ANON_KEY env vars, the viewer loads live from Supabase. Otherwise it falls back to ./thornkeep.json (static snapshot)."
}
